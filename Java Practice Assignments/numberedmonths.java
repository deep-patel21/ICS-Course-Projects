/* numberedmonths.java
 * Deep Patel
 * February 13th, 2018
 * program which transforms numbers into corresponding months */

import java.util.Scanner;

public class numberedmonths {
  public static void main(String[] args) {
    
  Scanner in = new Scanner(System.in);
  
  System.out.println("Enter a number between 1 and 12:");
  String input = in.next();
  String count = String.parseStr(input);
  
  System.out.println(count.substring(Jan),count.substring(Feb),count.substring(Mar),count.substring(Apr),count.substring(May),count.substring(Jun),count.substring(Jul),count.substring(Aug),count.substring(Sep),count.substring(Oct),count.substring(Nov),count.substring(Dec));
 }
}